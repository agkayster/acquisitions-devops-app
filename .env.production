# Production Environment Configuration
NODE_ENV=production
PORT=8000
LOG_LEVEL=info

# Neon Cloud Database Configuration (Replace with actual production URL)
DATABASE_URL=${DATABASE_URL}

# Security Configuration (Inject via environment or secrets management)
ARCJET_KEY=${ARCJET_KEY}

# JWT Configuration (Inject via environment or secrets management)
JWT_SECRET=${JWT_SECRET}
JWT_EXPIRES_IN=1h

# Production specific settings
DEBUG=false